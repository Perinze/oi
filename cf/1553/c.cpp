#include <bits/stdc++.h>
#define rep(i, l, r) for (int i = (int)l; i < (int)r; i++)
using namespace std;
using ll = long long;

int main() {
	int t;
	cin >> t;
	while (t--) {
		int x;
		cin >> x;
		cout << (x + 1) / 10 << endl;
	}
	return 0;
}
